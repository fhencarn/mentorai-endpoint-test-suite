# Platform Endpoints

These scripts are placeholders and have not been tested yet.

All platform endpoints require a valid `course_id` in the format `course-v1:org+course+run`. This could not be obtained with the current credentials.

## Status
All 5 endpoints are currently logged as `blocked` in the endpoint matrix pending a valid `course_id`.

- Update the `course_id` placeholder in each script